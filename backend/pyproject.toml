[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.0",
    "python-multipart>=0.0.21",
    "sse-starlette>=3.1.2",
    "uvicorn>=0.40.0",
]
